# Copy to .env.local for local development.
#
# Leave unset in production: the client then derives wss://<current host>/ from
# window.location, which is what the same-origin /play/ deployment wants.
VITE_WS_URL=ws://localhost:8080
