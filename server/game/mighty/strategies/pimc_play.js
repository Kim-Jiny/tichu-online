'use strict';

/**
 * Legacy strategy name kept for compatibility.
 *
 * PIMC's friend/play sampling is replaced by the deterministic oracle
 * evaluator over the actual live world.
 */

module.exports = require('./oracle');
