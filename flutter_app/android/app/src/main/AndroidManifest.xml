<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Deliberately NO android.permission.CAMERA. Profile photos are taken via
         image_picker, which fires ACTION_IMAGE_CAPTURE and hands off to the
         system camera app — that needs no permission from us. Declaring CAMERA
         would flip Android into REQUIRING a runtime grant for that same intent,
         so adding it "for completeness" breaks capture until the user accepts a
         prompt we never show. Same for READ_MEDIA_IMAGES and gallery picking. -->
    <application
        android:label="Tichu Online"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Both hosts. tichu.kr is where new invites are minted;
                     tichu.jiny.shop stays until every build carrying it is
                     gone, so links shared by older versions keep opening the
                     app. See docs/DOMAIN_MIGRATION.md. -->
                <data android:scheme="https" android:host="tichu.kr" android:pathPrefix="/invite" />
                <data android:scheme="http" android:host="tichu.kr" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="tichu.jiny.shop" android:pathPrefix="/invite" />
                <data android:scheme="http" android:host="tichu.jiny.shop" android:pathPrefix="/invite" />
            </intent-filter>
            <!-- Custom scheme, kept as a fallback for contexts where app links
                 do not fire — an in-app browser (KakaoTalk, Instagram) will
                 happily open tichuonline:// but often swallows an https app
                 link and renders the page itself.

                 A SEPARATE filter on purpose: autoVerify applies to the whole
                 filter, and a custom scheme has nothing to verify against.
                 Putting it in the block above would make Android fail
                 verification for the https hosts too, silently turning off
                 app links.

                 'tichuonline', not plain 'tichu': custom schemes are first
                 come first served with no registry, and a one-word scheme is
                 easy for another app to claim. Nothing uses this yet. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tichuonline" />
            </intent-filter>
        </activity>
        <!-- Kakao Login Redirect -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kakaod9b4b3cfc86537fed9a80a659641ad30" android:host="oauth"/>
            </intent-filter>
        </activity>
        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2707874353926722~8074696710"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
